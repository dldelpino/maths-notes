\documentclass[11pt]{report}

\usepackage{preamble}

\begin{document}

\pgfplotsset{ every non boxed x axis/.append style={x axis line style=-}, every non boxed y axis/.append style={y axis line style=-}} % elimina las flechas en los ejes y mantiene centradas las rayitas de los ejes (no darle muchas vueltas)

\begin{tikzpicture}
    % \begin{axis}[
    %     ticks = none,
    %     xmin = 0,
    %     xmax = pi/2,
    %     ymin = 0.7,
    %     ymax = 1.3,
    %     samples = 800,
    %     scale = 1
    % ]
    % \end{axis}
    \begin{axis}[
        title = {$N=100$},
        title style = {yshift=-16ex}, % pegar el título al eje x
        axis x line = center,
        axis y line = none,
        every x tick/.style = {black},
        xmin = 0,
        xmax = 1,
        xtick = {0, 1},
        xticklabels = {0, 1},
        % axis lines* = left,
        % xmin = 0,
        % xmax = pi/2,
        % ymin = 0.7,
        % ymax = 1.3,
        % samples = 800,
        % xtick = {0, pi/2},
        % ytick = {1},
        % xticklabels = {0, $\frac{\pi}{2}$, },
        % yticklabels = {1},
        % scale = 1,
        % legend cell align = {left},
        % legend pos = south east,
        % grid = major,
        % x tick label style = below,
        % y tick label style = left,
        % ticklabel style = {font = \footnotesize},
        % extra x ticks = {pi/40},
        % extra x tick labels = {$\frac{\pi}{40}$},
        % %extra x tick style = {xticklabel style = {above left}},
        % extra y ticks = {1.17897944},
        % extra y tick labels = {$G$},
        % %extra y tick style = {yticklabel style = {above left}}
    ]
    % \addplot[domain=0:pi/2, color=c3!50, line width=1.5pt]{1};
    % \addlegendentry{\footnotesize $ f$};
    \foreach \n in {1,...,100} {
        \addplot[excl, c3, mark size = 1.25pt] coordinates {({mod(\n*sqrt(2), 1)},0)};
    }
    
    % \addplot[domain=0:pi/2, color=c3, line width=1.5pt] {\RepeatFunction{1}{\pgfkeysvalueof{/pgfplots/fun terms}}};
    % \addlegendentry{\footnotesize $ S_{40}f$};
    \end{axis}
\end{tikzpicture}

\end{document}
