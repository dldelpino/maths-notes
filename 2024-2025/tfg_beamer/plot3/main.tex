\documentclass[11pt]{report}

\usepackage{preamble}

\begin{document}

\begin{tikzpicture}
    \begin{axis}[
        axis lines* = middle,
        xmin = -pi,
        xmax = pi,
        ymin = -10,
        ymax = 50,
        samples = 300, % si pongo más o menos salen picos
        xtick={-pi, 0, -3*pi/4, -pi/2, -pi/4, pi/4, pi/2, 3*pi/4},
        ytick={10, 20, 30, 40},
        xticklabels={-$\pi$, $0$},
        yticklabels={$10$, $20$, $30$, $40$},
        scale=1.3,
        legend cell align={left},
        grid = major,
        x tick label style = below left,
        y tick label style = above left,
        extra x ticks = {pi},
        extra x tick labels = {$\pi$},
        extra x tick style = {xticklabel style = {below right}},
        extra y ticks = {0},
        extra y tick labels = {$0$},
        extra y tick style = {yticklabel style = {above left}},
        ticklabel style = {font = \tiny},
    ]
    \addplot[domain=-pi:pi, color=c2, line width=2pt]{sin(deg((15+1/2)*x))/sin(deg(x/2))};
    \addlegendentry{\footnotesize $D_{15}$};
    \end{axis}
    \begin{axis}[
        ticks = none,
        xmin = -pi,
        xmax = pi,
        ymin = -10,
        ymax = 50,
        samples = 300,
        scale = 1.3
    ]
    \end{axis}

\end{tikzpicture}

\end{document}
