\documentclass[11pt]{report}

\usepackage{preamble}

\begin{document}

\begin{tikzpicture}
    \begin{axis}[
        xmin = 0,
        xmax = 4,
        ymin = -2,
        ymax = 2,
        samples = 200,
        scale=1.5,
        grid = major,
        extra x ticks = {},
        extra x tick labels = {},
        extra x tick style = {xticklabel style = {below right}, major tick length=0pt},
        extra y ticks = {},
        extra y tick labels = {},
        extra y tick style = {yticklabel style = {below left}, major tick length=0pt},
        tick label style={font=\scriptsize}
    ]
    \addplot[domain=0:4, color=c2, line width=2pt]{-x*(x-1)*(x-2)*(x-3)};
    \addplot[domain=0:4, color=red, line width=2pt]{x};
    \addplot[domain=0:4, color=red, line width=2pt]{0.2*x};
    \addplot[domain=0:4, color=red, line width=1pt, dashed]{(3/2-sqrt(5)/2)*x};
    \addplot[domain=0:4, color=red, line width=1pt, dashed]{6*x};
    \addplot[domain=0:4, color=red, line width=1pt, dashed]{0*x};
    \addplot[mark = none, gray, dashed] coordinates {(-1,-3) (-1,3)};
    \end{axis}
\end{tikzpicture}

\end{document}
