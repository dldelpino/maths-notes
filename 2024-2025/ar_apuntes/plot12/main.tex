\documentclass[11pt]{report}

\usepackage{preamble}

\begin{document}

\begin{tikzpicture}
    \begin{axis}[
        ticks = none,
        xmin = -2,
        xmax = 2,
        ymin = -2,
        ymax = 2,
        samples = 200,
        scale = 1.25
    ]
    \end{axis}
    \begin{axis}[
        axis lines* = middle,
        xmin = -2,
        xmax = 2,
        ymin = -2,
        ymax = 2,
        samples = 200,
        xtick={-1, -2, 2},
        ytick={-1, -2, 2},
        xticklabels={-1},
        yticklabels={},
        scale=1.25,
        legend cell align={left},
        grid = major,
        x tick label style = above left,
        y tick label style = below right,
        extra x ticks = {1},
        extra x tick labels = {1},
        extra x tick style = {xticklabel style = {above right}},
        extra y ticks = {1},
        extra y tick labels = {1},
        extra y tick style = {yticklabel style = {above right}}
    ]
    \addplot[domain=-2:-1/2, color=c3!50, line width=2pt]{0};
    \addlegendentry{$ y = f(x)$};
    \addplot[forget plot, domain=-1/2:1/2, color=c3!50, line width=2pt]{1};
    \addplot[forget plot, domain=1/2:2, color=c3!50, line width=2pt]{0};
    \addplot[forget plot, excl, c3!50] coordinates {(-1/2,1)};
    \addplot[forget plot, excl, c3!50, fill = white] coordinates {(-1/2,0)};
    \addplot[forget plot, excl, c3!50] coordinates {(1/2,1)};
    \addplot[forget plot, excl, c3!50, fill = white] coordinates {(1/2,0)};
    \addplot[domain=-2:2, color=c3, line width=2pt]{sin(deg(pi*x))/(pi*x)};
    \addlegendentry{$y = \widehat{f}(x)$};
    \end{axis}
\end{tikzpicture}

\end{document}
